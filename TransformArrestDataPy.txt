def azureml_main(frame1):
  import pandas as pd
  import os.path

## Set a flag to define the environment
  Azure = False 

## If in Azure, the data frame is passed to the function,
## If running in the IDE, load it from a local file
  if(Azure == False):
    pathName = "E:\School\CIS 3200-01\Term Project Presentation\Final"
    fileName = "Arrest_Data_from_2010_to_Present.csv"
    filePath = os.path.join(pathName, fileName)
    frame1 = pd.read_csv(filePath)

## Select a subset of columns
  frame1 = frame1[["Arrest Date", "Time", "Area Name", "Charge", "Charge Description"]]

## Filter and add a column to show totals for Van Nuys
  frame1 = frame1[frame1['Area Name']=='Van Nuys']
  frame1["Total"] = frame1["Area Name"]
  return frame1
